# .env.example
# 이 파일을 복사하여 .env.development 또는 .env.production으로 사용하고, 각 변수에 실제 값을 입력하세요.

# ==========================================
# Database Configuration
# ==========================================
DB_HOST=stockeye-db          # 데이터베이스 호스트명 (Docker: stockeye-db, 로컬: localhost)
DB_PORT=5432                 # 데이터베이스 포트 (기본: 5432)
DB_USER=postgres             # 데이터베이스 사용자명
DB_PASSWORD=your_db_password # 데이터베이스 비밀번호 (운영 환경에서는 강력한 비밀번호 사용)
DB_NAME=stocks_db            # 데이터베이스 이름

# PostgreSQL 컨테이너 초기화 변수 (docker-compose 사용 시)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=stocks_db

# ==========================================
# Telegram Bot Configuration
# ==========================================
TELEGRAM_BOT_TOKEN=your_telegram_bot_token  # 텔레그램 봇 토큰 (@BotFather에서 발급)
TELEGRAM_ADMIN_ID=your_telegram_user_id     # 텔레그램 관리자 ID (숫자)

# ==========================================
# External API Keys
# ==========================================
DART_API_KEY=your_dart_api_key  # DART OpenAPI 인증키 (https://opendart.fss.or.kr/)

# ==========================================
# Application Environment
# ==========================================
APP_ENV=development  # 애플리케이션 환경 (development, production, test)

# ==========================================
# Security Keys
# ==========================================
# Bot-API 간 인증을 위한 비밀 키 (최소 32자 이상의 무작위 문자열 권장)
BOT_SECRET_KEY=your_bot_secret_key_min_32_characters_long

# JWT 토큰 서명을 위한 비밀 키 (최소 32자 이상의 무작위 문자열 권장)
JWT_SECRET_KEY=your_jwt_secret_key_min_32_characters_long

# ==========================================
# Email (SMTP) Configuration
# ==========================================
SMTP_HOST=smtp.gmail.com              # SMTP 서버 호스트
SMTP_PORT=587                         # SMTP 서버 포트 (TLS: 587, SSL: 465)
SMTP_USERNAME=your-email@gmail.com    # SMTP 사용자명 (Gmail 주소)
SMTP_PASSWORD=your-app-password       # SMTP 비밀번호 (Gmail 앱 비밀번호)
SMTP_USE_TLS=true                     # TLS 사용 여부
SENDER_EMAIL=noreply@stockeye.com     # 발신자 이메일 주소
SENDER_NAME=StockEye                  # 발신자 이름
